# Run 2026-09-20-005 — Course & Question Authoring + Structured Import V1

PLAN_VERSION: 004
RUN_ID: 2026-09-20-005
BASE_HEAD: 6090862
STATUS: IN PROGRESS

---

## Slice S1 — Authoring Reality Audit + Domain Gap Map

MODE: INVESTIGATE. No commit (investigation-only per Plan).

Key findings:

- `courses` had no `status`/`exam_date` column; `join_policy` (separate concern) already existed.
- No `topics` table anywhere. `materials` table exists in the initial schema but is completely
  unreferenced by any application code — an unused placeholder for future Material/Source
  (Run 008), not the Topic model.
- `questions.current_version_id` is already nullable by design ("transient pre-first-version
  state") — decided to reuse this as the draft-before-first-publish state rather than adding a
  separate `QuestionDraft` table; draft content will live in new nullable `draft_*` columns on
  `questions` (S5/S6 decision).
- No instructor/admin UI exists at all; everything under `src/app` is learner-facing or
  `/login`/`/join`.
- No zod/papaparse/xlsx/csv-parse or any parsing/validation library installed. JSON import (S7)
  needs no new dependency; CSV (S8) will use a narrow hand-rolled parser; XLSX deferred per the
  Plan's own explicit fallback (no library installed, not narrowly justified yet).
- Authorization pattern to reuse: `CourseMembershipRepository.findMembership` +
  `isManagementRole`. Plan requires archived management memberships to ALSO fail closed for
  authoring (existing `setCourseJoinPolicy` only checks `revokedAt`) — new domain predicate
  needed (`canAuthorCourse`).

## Slice S2 — Course Lifecycle + Instructor Authorization Foundation

MODE: IMPLEMENT. STATUS: COMPLETE.

Delivered:

- Migration `20260926000000_course_lifecycle_v1.sql`: `courses.status`
  (DRAFT/PUBLISHED/ARCHIVED, existing rows backfilled to PUBLISHED, no leftover default) +
  `courses.exam_date` (nullable, metadata only).
- Domain: `canSelfJoinCourse`, `canAuthorCourse` (`src/domain/course/types.ts`).
- Application: `createCourse`, `getCourseForAuthoring`, `updateCourseMetadata`, `publishCourse`
  (DRAFT->PUBLISHED only), `archiveCourse` (DRAFT/PUBLISHED->ARCHIVED, terminal). `joinCourse`
  made lifecycle-aware (DRAFT/ARCHIVED never joinable even if OPEN).
- API: `POST /api/courses`, `GET`/`PATCH /api/courses/:courseId/manage`,
  `POST /api/courses/:courseId/publish`, `POST /api/courses/:courseId/archive`.
- Infrastructure: `PostgresCourseUnitOfWork` — `createCourse`'s two writes (Course +
  creator's OWNER membership) run in one transaction.

Discoveries / mid-slice findings:

- **DB review finding (fixed)**: `createCourse` was originally two independent, non-transactional
  writes; a partial failure would leave an orphaned Course row with no CourseMembership,
  permanently unmanageable by anyone (every authoring use case authorizes only through
  CourseMembership). Fixed with `CourseUnitOfWork`/`PostgresCourseUnitOfWork`, proven by
  dedicated PGlite rollback tests (`supabase/tests/postgres/postgres-course-unit-of-work.test.ts`).
- **Security review note (tracked, not fixed — dormant/unreachable)**: `canAuthorCourse` fails
  closed on archived management membership; the pre-existing `setCourseJoinPolicy` does not.
  No route currently wires `setCourseJoinPolicy`, so this is not exploitable today. Must be
  reconciled before any future Slice (likely S3) exposes it through an API route.
- Shared PGlite test harness (`supabase/tests/postgres/db-harness.ts`) and
  `supabase/tests/schema.integration.test.ts`'s own `insertCourse` helper both needed a `status`
  default — the new NOT NULL column with no default broke every existing test seeding a Course.
  Fixed (backward-compatible `overrides` param / explicit `'PUBLISHED'` default).

Reviewers: `unlock-db-reviewer` (BLOCKED -> fixed -> not re-reviewed independently after fix;
fix directly mirrors the already-reviewed `PostgresUnitOfWork` pattern and is proven by real
PGlite rollback tests), `unlock-security-reviewer` (APPROVED AFTER CORRECTIONS — one dormant,
non-blocking note, see above), `unlock-reviewer` (APPROVED AFTER CORRECTIONS — two trivial
doc/test-name corrections, both applied).

Tests: unit suite 713/713; schema/Postgres suite — see Test baseline below for final count.

Commit: (recorded after commit — see Git Log below)

---

## Test Baseline (as of end of S2)

- Unit tests: 713 / 713
- Schema/Postgres (PGlite): see final `npm run test:schema` run
- Typecheck: clean
- Lint: clean
- `git diff --check`: clean

---

## Git Log (Slice commits this Run)

- S1: no commit (investigation-only)
- S2: (pending — see `git log`)

---

## Manual Actions Required (running list)

- New migration `20260926000000_course_lifecycle_v1.sql` is committed locally only — not applied
  to hosted Supabase (per Plan/CLAUDE.md, Claude must not run `supabase db push`).
- Reconcile `canAuthorCourse` vs `setCourseJoinPolicy` archived-membership asymmetry before S3
  wires any join-policy-editing route.
