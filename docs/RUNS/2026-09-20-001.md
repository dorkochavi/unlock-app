# Run 2026-09-20-001

PLAN_VERSION: pre-Development-OS archival run
RUN_ID: 2026-09-20-001
BASE_HEAD: `14dc34c`
END_HEAD: `66df9f9`
STATUS: COMPLETE

> Historical archive of the autonomous UNLOCK run completed before Development OS V1 was introduced.
>
> This file is an immutable-style Run Report.
> It is NOT normal working context.
> Future Claude runs should not read it unless `docs/CHATGPT_PLAN.md` or the user explicitly authorizes it.

---

## Handoff Summary

Completed:

- Today answer submission end to end
- Interactive Today answering UI
- Today Skip flow
- Starter / New Material V1 product decision
- New Material fallback implementation
- OPEN Course learner onboarding
- Safe login return-path handling
- OPEN Course join authorization hardening
- Reviewer-driven DB / security / integration fixes
- Full local verification through the final Slice 6 state

Blocked:

- no code blocker at end of run
- hosted end-to-end verification of the newest flows is intentionally pending until remote migrations are applied by the user

Important discoveries:

- the existing mature `submitAnswer` path could be extended for DailyPlan rather than duplicated
- Manual Practice and Today must remain separate even when they reference the same Question
- New Material is best modeled as fallback-only in V1 rather than mixed with ordinary review candidates
- OPEN Course join must preserve existing OWNER / INSTRUCTOR memberships
- revoked memberships must fail closed until explicit rejoin semantics are decided
- malformed/non-UUID Course path parameters may still produce a generic `500` under an older broader API pattern; this was not introduced by this run

Decisions created:

- ADR-017 — Starter / New Material V1

Manual actions still required:

- apply committed remote migrations manually
- verify hosted answer flow
- verify hosted Skip flow
- verify hosted New Material
- verify complete OPEN Course join → Today learner flow

Recommended next:

- complete Development OS V1
- create a new `docs/CHATGPT_PLAN.md`
- apply pending hosted migrations with explicit user authorization
- run manual browser QA
- continue with learner-facing UX/demo polish and hardening

---

## Slice 1 — Today Answer Submission

Status: COMPLETE

Commit:

`e85670d` — add Today answer submission flow

Implemented:

- `POST /api/daily-plan/items/:itemId/answer`
- DailyPlanItem answer orchestration
- extension of the existing `submitAnswer` pipeline
- server-side derivation of authoritative Course / Question / QuestionVersion / DailyPlan identity
- DailyPlan item ownership validation
- answer idempotency
- immutable Attempt persistence
- DailyPlan item completion
- no learner-facing leakage of grading-only fields

Migration added:

`20260924000000_daily_plan_answer_attempts.sql`

Key persistence behavior:

- Attempts may reference DailyPlan / DailyPlanItem
- Attempt survives DailyPlan deletion with pointer columns cleared
- Attempt cannot simultaneously claim both legacy TodaySessionItem and DailyPlanItem ownership paths
- Manual Practice remains independent from Today completion

Review:

- `unlock-db-reviewer`
- `unlock-security-reviewer`

Material reviewer findings were resolved before commit.

Verification at Slice 1:

- unit: `537 / 537`
- schema/Postgres: `170 / 170`
- typecheck: clean
- lint: clean
- `git diff --check`: clean

Hosted answer submission was not performed.

---

## Slice 2 — Interactive Today Answering UI

Status: COMPLETE

Commit:

`d1fa05b` — Interactive Today answering UI

Implemented:

- one active pending Today item at a time
- SINGLE_CHOICE interaction
- MULTIPLE_CHOICE interaction
- explicit submit
- stable submission ID across retry for the mounted item
- correct / incorrect feedback
- explicit Continue action
- completion state
- auth-expiry handling
- stale/already-resolved item refetch behavior
- generic learner-safe error state

No schema changes.

Verification:

- unit suite remained green
- typecheck: clean
- lint: clean
- `git diff --check`: clean
- local HTTP smoke checks passed

Real authenticated browser answer-click-through remained for manual QA.

---

## Slice 3 — Today Skip

Status: COMPLETE

Commit:

`b8198d0` — add Today skip flow

Implemented:

- `POST /api/daily-plan/items/:itemId/skip`
- application Skip use case
- ownership fail-closed behavior
- atomic pending → skipped resolution
- no request body
- no Attempt
- no correctness evidence
- no mastery mutation
- no misconception mutation
- no scheduler mutation
- no replacement item
- learner UI Skip action
- immediate advance to next pending item

Verification:

- unit: `554 / 554`
- schema/Postgres suite passed
- typecheck: clean
- lint: clean
- `git diff --check`: clean

Real authenticated browser Skip remained for manual QA.

---

## Slice 4 — Starter / New Material V1 Decision

Status: COMPLETE

Commit:

`dd65559` — ADR-017 Starter/New-Material V1

Created:

`docs/DECISIONS/017-starter-new-material-v1.md`

V1 decision:

- unseen = no prior real Attempt for the Question
- missing UserQuestionProgress is not sufficient proof of unseen
- New Material activates only when zero ordinary Next Best Action candidates exist
- no review + New Material mixing in V1
- up to 3 unseen Questions
- deterministic selection
- no per-Course fairness quota
- planning unseen content is not evidence
- no fabricated UserQuestionProgress
- established `NEW_LEARNING` vocabulary reused

Open Questions #4 and #5 were resolved for V1.

Documentation-only slice.

---

## Slice 5 — New Material Fallback

Status: COMPLETE

Commit:

`34df388` — New-material fallback implementation

Migration added:

`20260925000000_daily_plan_new_material_v1.sql`

Implemented:

- DailyPlan persistence accepts:
  - `NEW_LEARNING`
  - `NEW_MATERIAL`
  - `UNSEEN_MATERIAL`
- Postgres unseen-question discovery
- direct Attempt-based unseen enforcement
- deterministic unseen ordering
- exact current QuestionVersion resolution
- fallback only when ordinary ranked candidate count is zero
- global deterministic top-3 selection
- no fake progress writes
- no change to the ordinary Next Best Action ranking model

Review:

- `unlock-db-reviewer`
- no blocking findings

Verification at Slice 5:

- unit: `562 / 562`
- schema/Postgres: `186 / 186`
- typecheck: clean
- lint: clean
- `git diff --check`: clean

Remote migration application was intentionally not performed.

---

## Slice 6 — OPEN Course Learner Onboarding

Status: COMPLETE

Commit:

`66df9f9` — add open-course learner onboarding

Implemented:

- public-safe `GET /api/courses/:courseId`
- authenticated `POST /api/courses/:courseId/join`
- `/join/[courseId]`
- safe login `next` return path
- OPEN Course learner self-join
- repeat join idempotency
- OWNER / INSTRUCTOR preservation
- AUTHORIZED_ONLY self-join blocked
- revoked membership fails closed
- successful onboarding flow leads to Today

Security hardening:

- auth-before-DB ordering tested through real route wiring
- safe redirect allowlist added
- learner-facing Course lookup restricted to public-safe fields

Reviewer follow-up added:

- OWNER / INSTRUCTOR preservation tests
- route auth-before-DB tests
- PGlite joinCourse integration coverage
- stale comment cleanup
- retry UI bug fix

Review:

- `unlock-security-reviewer`
- `unlock-reviewer`
- `unlock-db-reviewer`

Verification at final Slice 6 state:

- unit: `592 / 592`
- schema/Postgres: `194 / 194`
- typecheck: clean
- lint: clean
- `git diff --check`: clean
- working tree clean

---

## Commits in This Run

1. `e85670d` — add Today answer submission flow
2. `d1fa05b` — Interactive Today answering UI
3. `b8198d0` — add Today skip flow
4. `dd65559` — ADR-017 Starter/New-Material V1
5. `34df388` — New-material fallback implementation
6. `66df9f9` — add open-course learner onboarding

All six commits were later pushed successfully to:

`origin/feature/project-foundation`

Remote branch baseline after push:

`66df9f9`

---

## Verification Summary

Final local baseline:

- Unit tests: `592 / 592`
- Schema/Postgres PGlite tests: `194 / 194`
- Typecheck: clean
- Lint: clean
- `git diff --check`: clean

Verification scope includes:

- unit behavior
- application/use-case behavior
- route wiring
- auth-before-DB ordering
- PGlite Postgres integration behavior
- migrations through the local committed chain

Verification does NOT yet include the final hosted browser flow for all newly-added features.

---

## Pending Remote Migrations

The hosted Supabase project was previously applied through:

`20260923000000_auth_user_provisioning.sql`

The following committed migrations remain pending remote application:

1. `20260924000000_daily_plan_answer_attempts.sql`
2. `20260925000000_daily_plan_new_material_v1.sql`

These must be reviewed and applied only with explicit user authorization.

Claude must not run `supabase db push` automatically.

---

## Pending Hosted QA

After remote migrations are applied, manually verify:

- learner login
- OPEN Course join
- return-to-join redirect after login
- timezone behavior
- Today generation
- real learner-facing question content
- Today answer submission
- correctness feedback
- Continue
- Skip
- Today completion
- New Material fallback for a genuinely fresh learner
- repeat same-day Today stability

---

## Known Non-Blocking Gap

Malformed or non-UUID Course path parameters may still follow an older generic
API failure pattern and return a generic `500` rather than a cleaner validation /
not-found response.

This behavior:

- was not introduced by Slice 6
- did not block the onboarding flow
- was deliberately deferred instead of expanding scope

---

## End State

Application/product work is complete through Slice 6.

The repository then moved into Development OS V1 documentation restructuring.

Future development must be driven by a new `docs/CHATGPT_PLAN.md`.

Do not infer future execution order from this historical Run Report.
