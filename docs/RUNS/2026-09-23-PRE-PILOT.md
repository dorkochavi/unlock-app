# Run 2026-09-23-PRE-PILOT — Pre-Pilot Validation Run

Run: `2026-09-23-PRE-PILOT`

Status: `PARTIAL` — S1, S2 COMPLETE; S3 PASS; **S4 NOT COMPLETE** (preliminary device pre-check only; open blocker)

Baseline: `c5d370b` (`feature/project-foundation`)

Implementation head at closeout: `696ea66` (all Run commits already pushed by the human; this Run's closeout commit is local)

---

## 1. Run Goal

Prove the Ruppin pilot is technically safe enough to run and that one small, trustworthy
instructor signal (minimal Item Analysis) can be exposed from existing Attempt evidence,
without expanding into product Run 009. Independent Technical and Content Go/No-Go gates.

## 2. Scope Completed

- **S1** `2f2384f` — pure aggregate-disclosure policy (`src/domain/insights/`): minimum 5 active
  learners and 5 distinct responders; explicit ELIGIBLE / INSUFFICIENT_COURSE_SIZE /
  INSUFFICIENT_RESPONSES outcomes; counts only, no identity.
- **S2** `0cdc73d`, correction `cafa3f6` — instructor Item Analysis endpoint + page: OWNER/active
  INSTRUCTOR of a PUBLISHED Course; first Attempt per distinct active LEARNER on the current
  QuestionVersion (server `created_at` ordering, persisted `is_correct`); non-eligible items
  return no numbers; after the differencing note the payload was reduced to responder count +
  incorrect rate rounded to the nearest 10 points (no exact correct/incorrect counts); manual
  refresh only. No migration.
- **S3** — synthetic classroom burst sanity, **PASS**: local harness `627dc92`; hosted tooling
  `eb1f504`; hosted TLS fix `755eb72`; duplicate-answer semantics correction `7c5d135`;
  `DATABASE_POOL_MAX` `696ea66`. Hosted 30-learner result: see §6.
- Backlog: FUB-018..022 (post-pilot learning-visibility directions), FUB-023/024 (analytics
  follow-ups), FUB-025 (answer idempotency vs server-generated `answeredAt`), FUB-026
  (Today/Answer round-trip reduction).

## 3. Scope Not Completed

**S4 — Real-Device Rehearsal + Technical/Content Go/No-Go: PENDING / NOT COMPLETE.** Only a
preliminary pre-check was attempted (2026-09-24; iPhone 16 Pro Max, Chrome, Wi-Fi): real signup
succeeded; the confirmation email arrived in ~1.5 s; the confirmation URL redirected to
`http://localhost:3000` (phone hit `ERR_CONNECTION_REFUSED`); the account was nevertheless
confirmed and manual login succeeded; the original Course join had NOT happened, Courses was
empty, and Today showed its legitimate no-items state. Question/answer flow was not meaningfully
tested; Today is NOT classified as failed. **Open blocker** (undiagnosed, unfixed): signup
confirmation redirect target / lost join-return flow. Neither Go/No-Go gate has a result. Not
yet proven: signup/email throughput and rate limits, real phones on Wi-Fi/cellular, Hebrew/RTL,
double-tap behavior on a device, content sign-off.

**Addendum 2026-09-25 (append-only; the paragraph above is historical).** A later real-phone
rehearsal on Production (private browser, fresh learner, published self-join Course) succeeded
end to end: join → signup → confirmation email (~1 s) → login → Today with questions → Courses
shows the Course → correct and incorrect answers with "נכון"/"לא נכון" feedback + Continue.
FUB-027 is now HOSTED + MANUAL VERIFIED and F-12 MANUAL UI VERIFIED (both RESOLVED for current
Pre-Pilot scope). S4 is still NOT COMPLETE: one device only, protocol items unexecuted,
Technical Go/No-Go NOT YET COMPLETE, Content Go/No-Go NOT YET EXECUTED (no instructor
sign-off). Details: `docs/PILOT_READINESS.md` (full evidence tables: `git show afcd750:docs/CHATGPT_PLAN.md`, "S4 Real-Device Evidence #1").

**Addendum 2 (2026-09-25, append-only).** Further Production QA (Android second device,
Wi-Fi/cellular/switch, refresh/resume/double-tap, QR scan, F-13, F-14 (answer-submit failure/retry only; initial-load manual check not executed), instructor Item Analysis
with learner denial, Runtime Logs clean, ≈7 s existing-learner time to first answer) closed the
**Technical Go/No-Go: PASS** with recorded accepted gaps. **Content Go/No-Go: WAITING FOR REAL
PILOT MATERIAL** (external; not executed; no sign-off). The Run remains formally open on
Content; technically ready, not content-approved. Full table and deferred checklist:
`docs/PILOT_READINESS.md` (full evidence table: `git show afcd750:docs/CHATGPT_PLAN.md`, "S4 Real-Device Evidence #2"). Addendum 1's "Technical NOT YET COMPLETE"
statements are superseded.

## 4. Important Decisions

- Item Analysis semantics fixed by the Plan: current QuestionVersion only; first attempt per
  learner; aggregate-only; thresholds 5/5; neutral wording; manual refresh; no realtime.
- "Active LEARNER" = role LEARNER, not revoked/archived (existing `isActiveMembership`); Course
  must be PUBLISHED (same meaning DailyPlan uses).
- Privacy correction (human decision after review): coarse 10-point rate instead of exact counts;
  operating rule — Item Analysis is refreshed after a group answering window, never per answer.
- Hosted pool: `DATABASE_POOL_MAX=5`; code default stays 1 (range 1..10). Hosted CA supplied as
  `DATABASE_SSL_CA` contents. Today/Answer round-trip reduction deferred (FUB-026); answer
  idempotency semantics deferred (FUB-025). No product answer semantics changed.

## 5. Implementation Summary

`src/domain/insights/`; `src/application/insights/`; `src/infrastructure/postgres/
item-analysis-repository.ts`; `src/app/api/courses/[courseId]/item-analysis/`; instructor page
+ messages; `pg-ssl-config.ts` (CA/TLS resolution; `pg-connection-string` now a direct
dependency); `pg-pool-config.ts` (`DATABASE_POOL_MAX`, optional pool-queue logging);
`scripts/burst/**` + `supabase/tests/burst/**` (opt-in `npm run test:burst`, excluded from
`test:schema`).

## 6. Verification Evidence

- Unit: full suite 1227/1227 (126 files) after the last code change; targeted suites per Slice.
- Schema/PGlite: Item Analysis repository 7/7 (real Postgres semantics; not multi-connection).
- Local synthetic burst (real Supabase-image Postgres 17, 30 and 40 learners, both pool shapes):
  green; invariants one plan / one Attempt / one completed item per learner. Ceiling probe: 120
  one-connection pools against a 100-connection direct server → 27 `too many connections`.
- **Hosted S3, 30 learners, Vercel Preview + Supabase** (pre-confirmed accounts; each request pair
  doubled by the harness, so load ≈ 2× a real class):

  | | `DATABASE_POOL_MAX=1` | `DATABASE_POOL_MAX=5` |
  | --- | --- | --- |
  | Today p50/p95/max | 15.5 / 18.9 / 19.7 s | 3.07 / 4.85 / 4.87 s |
  | Answer p50/p95/max | 11.1 / 14.7 / 16.0 s | 2.11 / 3.18 / 3.29 s |
  | Wall clock | 37.3 s | 10.1 s |
  | Result | 30/30 ok, no 429/5xx/timeout | 30/30 ok, exit 0 |

  Diagnosis (read-only): `pg_stat_statements` 0.04–0.9 ms per statement; no cross-learner lock
  contention; queueing behind `Pool(max:1)`. Guidance (p95 ≤ ~5 s, max ≤ ~15 s) met only at 5.
- Not run: hosted 40 learners; any browser/E2E suite; real-device rehearsal (S4).
- Evidence labels: local synthetic (real Postgres, no HTTP/auth); hosted HTTP (Vercel + Supabase,
  scripted client on one IP). Neither proves signup/email, mobile UX, RTL or cellular.

## 7. Review Evidence

| Review | Target | Outcome |
| --- | --- | --- |
| security | S2 Item Analysis | no blocking; noted residual small-cell differencing → drove the coarse-rate correction |
| general | S2 | no blocking (non-blocking UI/refresh notes) |
| security re-review | S2 correction | no blocking; residual at n=5..9 stated plainly |
| security | S3 burst tooling | no blocking; added localhost-only guard on the local suite |
| security | TLS resolver, round 1 | **CORRECTIONS**: control-character parser disagreement (a TLS-verification bypass) and `?host=` making a remote host look local |
| security | TLS resolver, round 2 | no blocking after fixes (differential testing vs pg's real parser) |
| general | pool-size config | no blocking; blank-value handling tightened |

No specialist reviewer for S1 (pure domain), the harness-semantics correction, or the
backlog/docs commits (low risk per `review-commit`).

## 8. Telemetry / Development OS Audit

Measure → interpret → compare → act. No composite score. Raw data: `scratch/telemetry/
2026-09-23-PRE-PILOT/` (local, disposable). Native `/context` and `/insights` were not captured
by this closeout (user slash commands) — **unavailable**.

### 8.1 Performance / cost
- Events attributed to this RUN_ID: 262 (2026-09-23 18:30–22:07 UTC, ≈ 3h37m of activity).
- **Session-level (not separable):** duration 5h06m, API time 52m36s, estimated cost $20.94,
  +3,415/−92 lines. The session began with the Run 008 close-out ("Close Run 008 pilot gates"),
  so these cover more than this Run; Run-only cost/duration is **unavailable**.
- Model Sonnet 5, effort high, Claude Code 2.1.280.

### 8.2 Context / cache
- Highest ending context: 59% of a 1,000,000-token window (≈ 590K input tokens at last
  measurement; mid-run peak **unavailable**, only the ending value is recorded). Compactions 0.
- Cache: 198 requests, 1 miss (`ttl_expired_1h`), hit ratio 99.05%. No `/clear` events observed.

### 8.3 Tool activity (attributed events)
- Bash 135, Edit 26, Read 26, Write 25, Grep 6, Glob 1; tool failures 6 (Run 007: 3, Run 008: 0).
- Tool-response characters 1,029,854: Bash 393,734 (38%), Edit 354,334 (34%), Read 142,596 (14%),
  Write 139,814 (14%), Grep+Glob 5,317. Edit+Write echoes = 494,148 (48%).
- Verification-class activity (command classes; compound commands counted per sub-command):
  GIT 76, TEST 21, VERIFICATION 40, OTHER 906 (OTHER is dominated by generic shell work and is
  not further interpretable).
- Subagents: 7 dispatched (5 security, 2 general); 36 stop events (the count over-reports —
  stop events also fire for hand-back notifications). Telemetry hand-back characters
  (12,698 total) measure only what the hook saw and **under-report** the real reports (which
  were multi-KB each); treat as unreliable.
- Instruction loading: 0 events observed (loads occurred at session start, before this RUN_ID);
  layering effectiveness cannot be assessed from this telemetry — **unavailable**.

### 8.4 Context access (file reads)
- FILE_READ events 26, unique 19, re-reads 7 (27%); main-context Read-tool reads only 4, subagent
  reads 22. COLD files 0; historical Run files 0; no `docs/RUNS/**` read.
- **Blind spot (measured, important):** most main-context source reading was done with shell
  `cat`/`sed`/`grep`, which is recorded as Bash, not FILE_READ. The read counts above therefore
  undercount real source consumption and are **not comparable** to Run 007 (199 reads / 63%
  re-read) or Run 008 (157 / 48%), which used the Read tool. Shell reads per file: **unavailable**.
- Hottest files (all read by review subagents, twice each): the Item Analysis page/route/handler/
  use case/repository, `pg-pool.ts`, `pg-ssl-config.ts`. These are the Run's own new files under
  review — healthy hot files: re-reads follow relevant change (S2 → S2 re-review; TLS round 1 →
  round 2). Main-context startup files: `CHATGPT_PLAN.md`, `DEV_STATUS.md` once each.
- Re-read classification (evidence permits only): 7 re-reads = review/re-review after relevant
  change (justified) — no re-read is shown to be avoidable; intent otherwise unknown.
- Previous WATCH files (`domain/learning/answer.ts`, `import/types.ts`, in-memory fakes): not
  hot this Run (`answer.ts` was read once via shell for the grading audit). No new WATCH file.

### 8.5 Non-file context cost
- Edit/Write tool results are the largest single non-read contributor (48%). The largest
  results coincide with harness "file changed on disk" reminders that re-inject up to ~200 lines
  of a file after it was modified by a shell script (`node -e`/`sed`) between tool calls
  (≈ 10 occurrences in the transcript; the correlation with the 13.6K-char average Edit result
  is INFERENCE, not measured attribution).
- Largest shell outputs ~26–30K chars each (test/diff output). Reviewer hand-backs entered
  context in full (real size unmeasured, see above). Polling/wait loops: none (background
  agents notified on completion; no poll loops). Repeated `git status`: ≈ 76 git-class commands.

### 8.6 Comparison (only where comparable)
| | 007 | 008 | Pre-Pilot |
| --- | --- | --- | --- |
| Peak/ending context | 57% | 54% | 59% |
| Compactions | 0 | 0 | 0 |
| Cache hit | 99% | 100% | 99% |
| Tool failures | 3 | 0 | 6 |
| Edit/Write vs Read chars | echoes > reads | not binding | echoes 3.5× reads |
Cost, duration, reads and tool counts are **not comparable**: 007/008 were multi-Slice
implementation Runs with Read-tool-heavy sessions; this Run mixed hosted investigation, three
harness iterations, read-only diagnosis and user-directed micro-tasks, and its session/cost
figures are session-cumulative.

## 9. Quality / Process Audit

- **Planning and Slice boundaries:** four Slices executed in order, one commit per coherent
  unit; S4 not started once the human gate was reached; hosted mutation only on explicit
  authorization; no push by the agent (all pushes human).
- **Verification:** targeted first, full unit when shared modules changed (messages, pg-pool);
  evidence reused otherwise. Full unit suite run ≈ 8 times — each after a change touching shared
  or cross-cutting code; none clearly avoidable.
- **Review:** security review used where trust/secret/TLS/leak risk was material (5 dispatches);
  it found the two real TLS defects and the S2 disclosure residual. No reviewer for pure-domain
  or docs/harness-semantics changes.
- **Hosted vs local separation:** clean labelling, but two overclaims were made and later
  corrected: (a) the first S3 report said the local run proved duplicate-submit idempotency,
  when a one-connection pool and identical `answeredAt` had masked real HTTP behavior;
  (b) the pool `max:1` rationale in `pg-pool.ts` was reasoning, never measured for hosted.
- **Stop-and-investigate discipline:** every hosted failure (TLS ENOENT, answer 409, latency)
  was investigated read-only before any code change; fixes were configuration/harness-first
  (env var for pool size; TLS from env; harness expectation) — no Today/Answer refactor and no
  product semantics changes.
- **Rework:** TLS security corrections; three iterations of the local burst harness; shell-patch
  quoting failures (≥ 6, one corrupted regex found by typecheck); a 40-learner pool over-
  allocation.

### Near-misses
| # | Near-miss | Detected by | Escaped | Process change? |
| --- | --- | --- | --- | --- |
| 1 | Vercel `DATABASE_URL` carried a local `sslrootcert` path (ENOENT) | hosted Vercel Runtime Logs | Run 008 docs/verification (local-only TLS check; env parity never inspected) | Normal discovery; WATCH: hosted-env parity as an explicit preparation item |
| 2 | First TLS resolver had a verification bypass and a `?host=` locality gap | mandatory security review (differential test vs pg's real parser) | my own tests (asserted my output, not pg's merged result) | KEEP security review; test third-party parsing at the real merge point |
| 3 | Hosted duplicate-answer pair returned 409 | hosted smoke | local harness (serialized pool + identical `answeredAt`) | WATCH: harness realism |
| 4 | Local harness masked realistic HTTP concurrency | investigation of #3 | first local design | same root as #3 |
| 5 | Realistic local harness used 3 pools/learner → 25 failures at N=40 | immediate local run failure | none (self-inflicted test infra) | Normal discovery |
| 6 | `Pool(max:1)` assumed correct for hosted (Fluid concurrency) | hosted S3 latency + pg_stat_statements | Run 008 "connection strategy CLOSED" (no concurrency test) | KEEP the hosted burst gate; documented rationale ≠ evidence |
| 7 | Signup confirmation redirects to `http://localhost:3000`; join-return lost | preliminary real-device pre-check | every local/PGlite/scripted layer and Run 008's browser E2E (local app + pre-confirmed/hosted accounts) | KEEP the real-device gate; WATCH: environment-parity checks for auth redirects |

## 10. KEEP / WATCH / CHANGE

**KEEP** (strongly supported): risk-based mandatory security review for trust/secret/TLS changes
(2 of 2 TLS defects and the S2 residual found only there); hosted burst + real-device gates
(each found something no earlier layer did: #3, #6, #7); investigate-before-change on hosted
failures; config/tooling-first fixes over refactors; explicit hosted-mutation authorization and
human-only push; per-Slice focused commits; FUB capture for deferred work.

**WATCH** (one Run of evidence; none promoted):
1. Edit/Write echoes = 48% of tool-response characters (Run 007 yes, Run 008 no, now yes —
   cross-Run recurrence, but never binding: peak 59%, 0 compactions).
2. Telemetry blind spot: shell-based file reads are not FILE_READ (undercounted main reads).
3. Shell-embedded code patching (`node -e`, heredocs) → quoting failures and file-change
   re-injection; prefer Edit/Write for tracked files.
4. Harness realism: production request boundaries (separate connections, per-request clocks)
   must be modeled before claiming concurrency/idempotency evidence.
5. Hosted-environment parity (env vars, auth redirect URLs, provider rate/email limits) as an
   explicit preparation item before any hosted or device rehearsal.
6. Telemetry reliability: SubagentStop over-counts; hand-back characters under-report.

**CHANGE:** none justified now. Candidate for the next Run if #1–#3 recur: (a) `implement-slice`
skill — prefer Edit/Write over shell patch scripts; (b) `.claude/telemetry/collect.mjs` — classify
shell `cat`/`sed -n` on repo paths as FILE_READ. Owners noted; not applied (single-Run evidence
for #2/#3, and `.claude/**` changes are out of scope for a closeout).

## 11. Handoff for `/clear`

See `scratch/development_checkpoint.md` (local, one screen) and the "Pre-Pilot Validation Run"
section of `docs/DEV_STATUS.md`. Repository state at closeout: branch `feature/project-foundation`,
implementation HEAD `696ea66` pushed; closeout commit local.

**Design implications for the planned overnight run:** (1) do not schedule anything that touches
signup/auth redirects until the S4 blocker is diagnosed by the human/Claude with explicit scope;
(2) any overnight harness must model production request boundaries or label its evidence
"synthetic"; (3) avoid shell-side patching of tracked files; (4) keep hosted mutation out of
autonomous scope; (5) treat Progress as absent (Run 009) in any device or UX acceptance.
