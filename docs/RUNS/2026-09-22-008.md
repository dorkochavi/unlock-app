# Run 2026-09-22-008 — Authoring Integration + Pilot Readiness

PLAN_VERSION: RUN-008-AUTHORING-INTEGRATION-PILOT-READINESS
RUN_ID: 2026-09-22-008
BASE_HEAD: d509987 (`feature/project-foundation`, matches
`origin/feature/project-foundation` at Run start — Run 007's 8 commits
had already been pushed after its own Run Report was written, so this
Run's actual baseline differed from that report's "not pushed" note;
repository reality overrode it, per `.claude/rules`)
END_HEAD: b86d4e3 (local, not pushed)
STATUS: IMPLEMENTATION COMPLETE — PILOT MANUAL GATE PENDING

## Handoff Summary

Run 008 closed the small set of high-value hardening issues a prior
repository audit found (safe review-bundle tooling, real calendar-date
validation, Structured Import row-count limit, authenticate-before-
body-parsing across every authenticated JSON route), confirmed the
instructor authoring workflow built across Runs 005-007 is already
substantially coherent with only minor polish gaps, closed a real
learner-eligibility gap (an archived Course's content kept being pooled
into a learner's DailyPlan indefinitely), and produced one integration-
level proof of the complete instructor-to-learner authoring lifecycle
against real Postgres. S6's pilot-readiness assessment found no
code-level blocker — every remaining item is a human-actionable manual
gate, primarily because this environment's `.env.local` points at a
remote (hosted) database/Supabase project that Claude will not
autonomously write test data into. See `docs/DEV_STATUS.md`'s "Pilot
Readiness" section for the full gate list — not duplicated here.

## Slices

- **S1** (`38e26fc`, plus a Plan-only baseline commit `1bea327`) —
  audit-derived hardening: `scripts/create-review-bundle.mjs` (safe,
  deterministic repository snapshot built from `git ls-files`, not a raw
  directory copy — found and fixed a real live bug where its own output
  under `scratch/` was being picked up by `tsc`/`eslint`, since only
  `node_modules` was excluded before); `.env.example` reconciled against
  the real Supabase/Postgres architecture; `src/lib/date-only.ts`
  (`isValidDateOnly`, real calendar-date validation replacing a
  shape-only regex for Course `examDate`, correctly handling the
  ECMAScript legacy two-digit-year quirk); `MAX_IMPORT_ROWS` row-count
  limit for Structured Import (resolving the other half of FUB-005);
  authenticate-before-body-parsing reorder across all 10 authenticated
  JSON routes in `src/app/api/**` (not just the two originally-audited
  import routes — the same buggy shape existed identically in all 10);
  `confirmImport`'s Phase 2 concurrency limitation documented against the
  real transaction, not merely asserted. Reviewed by
  `unlock-security-reviewer` (mandatory, auth-ordering) and
  `unlock-reviewer`: both NO BLOCKING FINDINGS. Two non-blocking findings
  fixed before commit (the date-only year-range edge case; the
  review-bundle script's symlink-following gap); a third (8 more routes
  lacking an explicit ordering-regression test beyond the 2 audited ones)
  addressed by adding the same test to all 8.
- **S2** (no commit — pure investigation, folded into S3's Slice
  description per the Plan) — traced the real instructor↔learner UI/API
  journey by reading the actual source. Finding: the flow is already
  substantially coherent — Course → Topics → author/import → publish
  Questions → publish Course → share → learner join → Today is reachable
  from one hub page with no SQL/internal-ID knowledge anywhere in the
  traced path. Only minor-polish gaps found, nothing blocking the
  Roadmap exit condition.
- **S3** (`4fe5adb`) — instructor workflow polish informed directly by
  S2's findings: the Course hub's publish hint now states explicitly that
  publishing the Course does not publish its Questions, with a "X of Y
  questions published" summary shown near the Publish action; the import
  page distinguishes `SOURCE_TOO_LARGE` from `TOO_MANY_ROWS` (Run 008
  S1.D) instead of always showing the same "file too large" copy. Copy
  and a client-side derived count only — no production/authorization
  behavior changed.
- **S4** (`a894f9c`) — learner eligibility / draft-leakage invariant. A
  read-only inspection verified 7 named Roadmap-invariant claims against
  the real implementation: 6 were already safe (several structurally
  unreachable, not merely filtered). One real gap: `getOrCreateDailyPlan
  ForToday`'s `eligibleCourseIds` derivation filtered active memberships
  by role and by `CourseMembership.archivedAt`/`revokedAt` (a per-learner
  fact, ADR-015) but never checked the Course's OWN `status` (Run 005's
  DRAFT/PUBLISHED/ARCHIVED lifecycle) — a learner who joined while a
  Course was PUBLISHED kept having that Course's content pooled into
  their DailyPlan indefinitely after an instructor archived it. Grounded
  in already-accepted policy, not invented: `canSelfJoinCourse`'s own doc
  comment already stated this rule for join time; this Slice enforces it
  consistently for an existing membership too. Fixed via a new
  `CourseRepository.listStatuses` port method, wired through the real
  composition root, with regression evidence at both the in-memory and
  real-Postgres/PGlite level. Also closed a related, pre-existing
  test-coverage gap the same inspection found: the ordinary
  ranked-candidate path's draft-only-Question skip (unchanged code) had
  no regression test, only the unseen path did. Reviewed by
  `unlock-security-reviewer` and `unlock-db-reviewer` (both mandatory):
  both NO BLOCKING FINDINGS. The security reviewer's one non-blocking
  rigor note (the first two regression tests both had a ranked-progress
  candidate that always won, so the unseen/fallback branch was never
  independently isolated) was fixed before commit by adding an isolated
  variant at both the in-memory and real-Postgres level.
- **S5** (`b86d4e3`) — integrated instructor-to-learner authoring proof.
  One real-PGlite/Postgres integration test
  (`run-008-authoring-integration-walkthrough.test.ts`) proving the
  cross-Run loop no prior Run needed to prove together: Course creation
  through the real `createCourse` (Run 005) → Topics → manual authoring
  (Run 006) + Structured Import (Run 007) side by side, plus a third
  Question deliberately left in draft → all three `DRAFT_ONLY` before
  publish → publish the two through the existing, unmodified
  `publishQuestion` → publish Course → a real learner joins through
  `joinCourse` (Run 005) → only the two published Questions are
  learner-eligible (proven at both the unseen-question read path and the
  full `getOrCreateDailyPlanForToday` pipeline, composing correctly with
  S4's fix) → the draft-only Question never leaks through either path →
  re-edit and re-publish, proving the original `QuestionVersion` row is
  untouched and `current_version_id` now points at the new version.
  Reviewed by `unlock-reviewer`: one CORRECTIONS REQUIRED item (a raw-SQL
  timezone write contradicted the file's own "real use case only, except
  `insertUser`" claim) — fixed by using the real `setUserTimezone`
  application function instead.
- **S6** (no commit — assessment folded into `docs/DEV_STATUS.md`'s
  "Pilot Readiness" section) — pilot-readiness classification. `npm
  audit`: 0 vulnerabilities across 529 dependencies. Hosted migration
  state unchanged from before this Run (3 migrations still pending hosted
  application, none introduced this Run). Browser/hosted E2E: identified
  as a `MANUAL PILOT GATE`, not attempted — this environment's
  `.env.local` points at a remote (hosted) database, and creating real
  test users/Courses there autonomously would be exactly the kind of
  hosted-database mutation `CLAUDE.md` §6 puts out of bounds. Runtime
  troubleshooting posture, backup/restore, and Postgres/Vercel connection
  strategy each classified per `docs/DEV_STATUS.md`'s "Pilot Readiness"
  section — no code-level `PILOT BLOCKER` found in any of them.
- **S7** (this commit) — Run close: `docs/DEV_STATUS.md` current-state
  rewrite, this Run Report, Follow-Up Backlog already updated in S1, final
  git state below.

## Tests

- Typecheck: clean (full repo, re-verified after every Slice).
- Lint: clean (0 errors throughout; one pre-existing, unrelated warning
  in `.claude/telemetry/statusline.mjs`, never touched by this Run).
- Unit: full suite reused/rerun repeatedly through the Run (S1's
  auth-ordering reorder and S4's DailyPlan-eligibility change each
  touched enough shared surface to justify a full rerun per
  `.claude/rules/testing.md` §5, not merely targeted evidence) — final
  full run **1073/1073 passed, 120 files**.
- Schema/PGlite (real Postgres via `npm run test:schema`): final full run
  **266/266 passed, 25 files** — includes the new Run 008 S5 integrated
  walkthrough and the S4 archived-Course regression tests (both the
  ranked-path and isolated-fallback-path variants).
- `npm audit`: 0 vulnerabilities, 529 dependencies (45 prod, 446 dev, 116
  optional).
- Browser/E2E: not performed this Run — see S6 above and
  `docs/DEV_STATUS.md`'s "Pilot Readiness" section for the exact reason
  and the exact manual gate/reproduction steps.

## Reviewer Findings (all resolved)

- S1: `unlock-security-reviewer` (mandatory) NO BLOCKING FINDINGS on the
  10-route auth-ordering reorder; `unlock-reviewer` NO BLOCKING FINDINGS
  on the rest of S1, with two non-blocking findings (date-only.ts's
  two-digit-year edge case; the review-bundle script's symlink-following
  gap) fixed before commit, and a third (extend the ordering-regression
  test beyond the 2 originally-audited routes to all 10) addressed by
  adding it to the remaining 8.
- S4: `unlock-security-reviewer` and `unlock-db-reviewer` (both
  mandatory) NO BLOCKING FINDINGS. The security reviewer's one
  non-blocking rigor note (fallback/unseen path not independently
  isolated by the first two regression tests) fixed before commit by
  adding an isolated variant at both the in-memory and real-Postgres
  level.
- S5: `unlock-reviewer` CORRECTIONS REQUIRED — one raw-SQL write
  contradicted the file's own "real use case only" claim — fixed by
  using the real application function instead.

No BLOCKER finding was raised at any point in this Run. Both mandatory
security reviews (S1.E, S4) and the mandatory DB review (S4), required by
the Plan's Security-Sensitive Autonomous Guardrail, returned NO BLOCKING
FINDINGS.

## Local vs. Hosted Boundary

No new migration was introduced this Run. No hosted Supabase action was
performed or required. No hosted/browser interactive verification was
performed — this environment's `.env.local` points at a remote (hosted)
database/Supabase project, not an isolated local sandbox, and Claude does
not autonomously write test data (real users, real Courses) into what is
almost certainly the shared pilot project. This is recorded as a
`MANUAL PILOT GATE`, not claimed as proven.

## Manual Actions Required

1. Push local HEAD (`b86d4e3`) to `origin/feature/project-foundation`
   when ready (5 commits ahead, currently unpushed).
2. Resolve the pilot-readiness manual gates in
   `docs/DEV_STATUS.md`'s "Pilot Readiness" section: browser/hosted E2E
   proof (either point a disposable local Postgres+Supabase project at
   this repo and run `npm run dev`/`npm run test:e2e`, or confirm the
   current hosted target is safe to write pilot-shaped test data into);
   apply the three pending migrations to the hosted project; verify the
   hosted project's backup/restore plan; confirm `DATABASE_URL` uses
   Supabase's connection pooler endpoint before a Vercel deployment.
3. Decide whether to experiment with the `CHANGE CANDIDATE` Development
   OS observation recorded in `docs/DEV_STATUS.md` (named-negative-case
   test isolation, now observed cross-Run) before it is absorbed into
   `.claude/rules/testing.md`.

## Deferred / Out of Scope (confirmed not implemented this Run)

Run 009 learner progress/insights; Run 010-012 work; CI/CD platform;
broad observability/APM; backup automation; broad rate limiting/CSP
redesign; bulk import persistence optimization; global Unit-of-Work/test-
fakes abstractions; broad source-comment cleanup; legacy TodaySession
deletion; AI/PDF ingestion; XLSX import; import auto-publish; Topic
auto-creation; re-import merge; bulk publish-all; Learning Engine
redesign; RLS introduction; hosted Supabase mutation; Git push. All
captured, where genuinely useful, as `docs/FOLLOW_UP_BACKLOG.md` items
FUB-006 through FUB-016 (S1) — capture only, no scope expansion.

## Telemetry / Development OS Summary

Source: `scratch/telemetry/2026-09-22-008/summary.md` (1 session, 533 raw
events — this entire Run ran as one continuous autonomous session, unlike
Run 007's 2 sessions).

- **Duration/cost**: session duration 1h 19m; API duration 48m 56s;
  estimated cost ~$34.
- **Context stayed well within budget for the whole Run**: highest ending
  context usage 54%, 0 compactions, 100% average prompt-cache hit ratio,
  0 reported cache misses — a full multi-Slice Product Run (Plan +
  6 Slices, 5 real commits, 2 mandatory security reviews, 1 mandatory DB
  review, 1 general review, one ~340-line cross-Run integration test) fit
  in a single session without needing a context reset, consistent with
  `CLAUDE.md`'s CLEAR-discipline guidance ("do not clear mid-Slice...
  the objective is not to maximize the number of clears").
- **Reviewer selection scaled with risk, matching Run 007's own
  precedent**: S1.E (auth-ordering across 10 routes) got the mandatory
  security reviewer plus a general reviewer for the rest of S1; S4
  (learner-eligibility/persistence change) got both mandatory security
  AND DB reviewers; S3 (pure UI copy, no production behavior change) got
  no reviewer at all, a deliberate risk-based choice, not an oversight;
  S5 (test-only, no production code) got one general reviewer as a
  sanity check, matching the precedent Run 006/007's own walkthrough
  tests received. No Slice received reviewers "by default" — each
  selection matched its actual changed-surface risk.
- **Full-suite reruns were used, not avoided, when cross-cutting risk
  justified them** — a deliberate departure from Run 007's own "avoid
  full-suite reruns" observation: S1's 10-route auth-ordering reorder and
  S4's DailyPlan-eligibility-generation change both touched enough shared
  surface (`.claude/rules/testing.md` §5's own stated triggers — "many
  test surfaces may be affected") that targeted-only evidence would have
  been the wrong call, not the disciplined one. Full schema/PGlite suite
  evidence was produced for both Slices' final verification, not merely
  the one changed test file, for the same reason.
- **Background-subagent discipline held**: every reviewer/audit dispatch
  ran as a background subagent; the main session did independent forward
  work (starting the next Slice's investigation, or fixing a
  already-known non-blocking finding) while waiting, rather than polling
  — consistent with `CLAUDE.md` §13.
- **One repeated inefficiency, now confirmed cross-Run — see the
  `CHANGE CANDIDATE` promotion in `docs/DEV_STATUS.md`**: Run 007 S4/S6
  each surfaced a missing negative-case test only at review; Run 008 S4
  repeated the same pattern in a different, more specific form (a test
  that asserted the right THING but via a code path that didn't actually
  isolate the branch under test). Two Runs, same category of gap — this
  now crosses `docs/DEVOS_OBSERVABILITY.md` §6's cross-Run bar for a
  policy-change candidate. Recorded as `CHANGE CANDIDATE`, not yet
  absorbed into `.claude/rules/testing.md` — that edit is left as an
  explicit human/future-Run decision, not made unilaterally this Run.
- **Telemetry limitations** (same as Run 007's own stated caveats):
  telemetry records metadata (tool names, durations, file paths), not
  file contents or prompt/response bodies; search/Grep discovery does not
  count as a substantive file read; session duration is not guaranteed to
  equal human elapsed Run time.

## Final Git State

Branch `feature/project-foundation`, local HEAD `b86d4e3`, 5 commits
ahead of `origin/feature/project-foundation` (`d509987`): `1bea327`,
`38e26fc`, `4fe5adb`, `a894f9c`, `b86d4e3`. Working tree clean (only
disposable `scratch/**` output present, already gitignored). Not pushed.

## Recommended Next Step

Run 008 is `IMPLEMENTATION COMPLETE — PILOT MANUAL GATE PENDING`. Every
autonomous Slice is done, reviewed, and evidenced; no code-level pilot
blocker exists. The Ruppin pilot itself cannot start until the manual
gates above are resolved by a human. Run 009 (Learner Progress +
Instructor Insights) requires its own new Plan; starting it does not
require Run 008's manual gates to be resolved first, but the pilot does.
