# Run Report — 2026-09-20-002

PLAN_VERSION: 001
RUN_ID: 2026-09-20-002
BASE_HEAD: `d67371a`
END_HEAD: `d67371a` (unchanged — this Run made no code changes)
OVERALL_STATUS: COMPLETE — stopped at `MANUAL_REMOTE_GATE` as required

---

## Handoff Summary

This Run investigated and verified — it did not implement new product behavior. Both slices in scope (S1, S2) concluded that the existing repository state already satisfies the Run's goal: the two pending hosted migrations are safe to apply, and the local demo journey is already protected by adequate tests with no discovered defects. Consequently S3 (defect hardening) had nothing to fix, and no commit was created. The working tree remains exactly as it was at Run start, except for the Plan's own expected `docs/CHATGPT_PLAN.md` replacement (untouched by Claude) and this Run's `docs/DEV_STATUS.md` update plus this report.

The remaining work is entirely Dor's manual step: authorize and apply the two pending migrations to hosted Supabase, then run the hosted QA checklist below.

---

## Slices

### S1 — Pending Hosted Migration Readiness — COMPLETE

Reviewed `20260924000000_daily_plan_answer_attempts.sql` and `20260925000000_daily_plan_new_material_v1.sql` against the already-hosted chain (through `20260923000000_auth_user_provisioning.sql`), `.claude/rules/postgres.md`, and the Plan's specific invariant checklist.

Findings: both migrations are forward-only, purely additive, chronologically ordered, and do not edit any accepted historical migration. The Attempt/DailyPlan linkage mirrors the existing `today_session_item_id`/`today_session_id` precedent exactly (composite FK ownership + MATCH FULL plan-consistency + column-scoped `ON DELETE SET NULL`), and a new CHECK constraint (`attempts_today_or_daily_plan_item_exclusive`) makes the legacy TodaySession path and the new DailyPlan path mutually exclusive per Attempt. The New Material migration only widens two closed CHECK lists (`action_type`, `tier`) by exactly the two values ADR-017 already needs at the application layer; it encodes no fallback/selection policy itself.

No DB blocker. No corrective migration needed. No `PLAN_CONFLICT`.

### S2 — Demo Journey Verification — COMPLETE

No usable browser/E2E harness exists in this repo (Vitest + PGlite only — confirmed via `package.json`); per the Plan, no new E2E framework was added.

Every non-UI "Must verify" item in the Plan's join → Today → answer → Skip → completion checklist was individually confirmed against actual existing test bodies (not inferred from file/test names): repeated-join idempotency, AUTHORIZED_ONLY fail-closed, revoked-membership fail-closed, same-day DailyPlan reuse, grading-data non-leakage before submission, answer/Skip retry idempotency, resolved-item immutability, and New Material's deterministic-up-to-3/no-fake-evidence behavior are all directly proven at the unit, application, or PGlite layer.

The only unproven layer is pure browser-level UI wiring (client-side redirect navigation, feedback/completion rendering) — not testable without new tooling the Plan explicitly disallows adding for this Run. This is covered by the manual browser QA script below instead.

No demo-blocking defect was found. No code changed.

### S3 — Narrow Demo Blocker Hardening — COMPLETE (no-op)

S2 found no demo-blocking defects, so there was nothing in scope to fix.

### S4 — Release Gate and Handoff — COMPLETE

`docs/DEV_STATUS.md` updated with current reality (migration-review outcome, demo-journey audit result, confirmed-unchanged test baseline). This report created. Checkpoint verification re-run and green. See below for the manual handoff.

---

## Commits

None. This Run made no source or Plan changes; only `docs/DEV_STATUS.md` and this report were written.

---

## Tests

- `npm run test:schema`: **194 / 194** (PGlite, real committed migration chain including both pending migrations)
- `npm test`: **592 / 592**
- `npm run typecheck`: clean
- `npm run lint`: clean
- `git diff --check`: clean

No values changed from the pre-Run baseline in `docs/DEV_STATUS.md` — confirmed, not assumed.

---

## Reviewer Outcomes

- `unlock-db-reviewer` on both pending migrations: **APPROVED FOR CHECKPOINT**. No BLOCKER. One non-blocking documentation note, reproduced under Discoveries below.
- `unlock-reviewer` final Run-level review of this Run Report and the `docs/DEV_STATUS.md` update: **APPROVED FOR CHECKPOINT**. No BLOCKER. Independently re-verified HEAD SHA, test counts, the PG18/PG15 engine-version claim (ran PGlite directly), migration file paths, and confirmed no scope leakage.

---

## Discoveries

- The PGlite test engine reports itself as PG18-equivalent while `supabase/config.toml` pins the hosted project to PG15. The specific mechanisms both pending migrations rely on (PG15+ column-scoped `ON DELETE SET NULL`, standard unnamed-CHECK-constraint auto-naming) are stable across PG15–18 and not a functional risk, but local proof is technically on a newer engine than the hosted target. Flagged in the Manual Remote Action section below rather than treated as a defect.

## Blockers

None code-side. Hosted end-to-end verification remains intentionally blocked until Dor authorizes the remote migration push (unchanged from pre-Run state).

---

## Manual Remote Action

Perform when ready — none of this was run by Claude:

1. Confirm local state: `git status -sb` should show `feature/project-foundation...origin/feature/project-foundation` with no unexpected divergence, and `git log --oneline -5` topped by `d67371a`. Push manually if/when you choose — Claude does not push.
2. Apply the two pending migrations to hosted Supabase, in this exact order (both already committed, no local edits needed):
   - `supabase/migrations/20260924000000_daily_plan_answer_attempts.sql`
   - `supabase/migrations/20260925000000_daily_plan_new_material_v1.sql`
   Use your normal authorized `supabase db push` (or equivalent) flow against the hosted project. Local proof is PGlite (PG18-equivalent engine); the hosted target is PG15 — the mechanisms both migrations use are stable across that range, but treat the push itself, not this Run's local proof, as your PG15 confirmation.
3. Confirm success: verify both migrations appear applied (e.g. `supabase migration list` against the linked project, or equivalent), and that no error was reported mid-push.
4. Proceed to the Hosted QA Checklist below.

---

## Hosted QA Checklist

Separate what each step actually proves:

**Locally verified (unit/application, this Run):** join idempotency, AUTHORIZED_ONLY fail-closed, revoked-membership fail-closed, grading-data non-leakage, answer/Skip idempotency, resolved-item immutability, New Material determinism.

**PGlite/PostgreSQL verified (this Run):** both migrations apply cleanly against the real committed chain; all of the above invariants proven at the constraint/repository level under a real (if newer-version) Postgres engine.

**Hosted verification still required — manual browser QA after step 2 above:**

- [ ] existing learner login (real Supabase Auth)
- [ ] OPEN Course join via `/join/[courseId]`
- [ ] join retry (reload/re-click) is idempotent — no duplicate membership, no error
- [ ] unauthenticated join redirects to `/login?next=/join/[courseId]` and returns to that exact path after login (never an external/unsafe target)
- [ ] Today first open for a learner with no prior plan today
- [ ] same-day reopen (reload Today) returns the identical persisted plan
- [ ] answer one item — correct/incorrect feedback renders, item resolves
- [ ] refresh mid-flow / duplicate submit is safe (idempotent, no duplicate Attempt, no crash)
- [ ] Skip one item — resolves without feedback, no correctness implied
- [ ] completion screen appears once all items are resolved, with no endless regeneration
- [ ] a learner with zero ordinary NBA candidates receives a New Material fallback plan (at most 3 items)
- [ ] confirm (via DB inspection or admin view, not guesswork) that Attempts/progress rows appear only for real answered items, never for Skipped or merely-planned New Material items
- [ ] confirm Skip produced no Attempt row and no mastery/misconception mutation for that item

---

## Recommended Next Run

After Dor completes the manual remote action and hosted QA above: a new Run focused on fixing only real hosted-environment defects found during that QA, plus remaining demo/visual polish. That Run needs a new `docs/CHATGPT_PLAN.md` from Dor/ChatGPT — do not infer it from this report.
